#pragma once
#include "calc.h"

/* ============================================
   Layout — სად დგას თითოეული ღილაკი
   და რომელ ღილაკს ხვდება მოცემული წერტილი
   ============================================ */

   /* რომელ ღილაკზეა წერტილი (x, y)?  ID_NONE — არცერთზე */
int HitTest(int x, int y);

/* ID-ით ღილაკის პოვნა (nullptr — თუ არ არსებობს) */
const Button* FindButton(int id);